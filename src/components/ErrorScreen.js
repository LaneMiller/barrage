import React from 'react';

const ErrorScreen = (props) => {
  return (
    <div className='error-screen'>
      <h4>Hmmm... there was a problem with the server.</h4>
      <p>Press Cmd + R to try again.</p>
    </div>
  )
}

export default ErrorScreen;
